{% extends 'base.html.twig' %}

{% block title %}
    Add a Wish
{% endblock %}

{% block body %}
    <div>
        {{ form_start(wishForm) }}
        {{ form_widget(wishForm) }}
        <button>Create!</button>
        {{ form_end(wishForm) }}
    </div>
{% endblock %}